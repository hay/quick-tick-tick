{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "quick-tick-tick",
  "title": "Quick TickTick",
  "description": "Quickly add TickTick tasks using the API (without the app)",
  "icon": "extension-icon.png",
  "author": "hayify",
  "owner": "haykranen",
  "platforms": [
    "macOS",
    "Windows"
  ],
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "add-to-inbox",
      "title": "Add to Inbox",
      "description": "Add a task to inbox",
      "mode": "no-view",
      "arguments": [
        {
          "name": "title",
          "placeholder": "Task title",
          "type": "text",
          "required": true
        }
      ]
    },
    {
      "name": "add-to-list",
      "title": "Add to Favorite List",
      "description": "Add a task to your favorite list",
      "mode": "no-view",
      "arguments": [
        {
          "name": "title",
          "placeholder": "Task title",
          "type": "text",
          "required": true
        }
      ]
    },
    {
      "name": "add-to-any-list",
      "title": "Add to Any List",
      "description": "Add a task to any list",
      "mode": "view"
    },
    {
      "name": "set-default-list",
      "title": "Set Default List",
      "description": "Choose which TickTick list the 'Add to Favorite List' command uses",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "clientId",
      "title": "Client ID",
      "description": "Your TickTick OAuth app Client ID. Create one at https://developer.ticktick.com/manage",
      "type": "textfield",
      "required": true
    },
    {
      "name": "clientSecret",
      "title": "Client Secret",
      "description": "Your TickTick OAuth app Client Secret",
      "type": "password",
      "required": true
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.6",
    "@raycast/utils": "^1.17.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^2.0.4",
    "@types/node": "22.13.10",
    "@types/react": "19.0.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "build": "ray build",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "prepublishOnly": "echo \"\\n\\nIt seems like you are trying to publish the Raycast extension to npm.\\n\\nIf you did intend to publish it to npm, remove the \\`prepublishOnly\\` script and rerun \\`npm publish\\` again.\\nIf you wanted to publish it to the Raycast Store instead, use \\`npm run publish\\` instead.\\n\\n\" && exit 1",
    "publish": "npx @raycast/api@latest publish"
  }
}